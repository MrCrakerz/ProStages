{% extends 'base.html.twig' %}

{% block body %}
<!-- Tableau Bootstrap -->
<table class="table table-hover table-dark">
  <thead>
    <tr>
      <!-- Titre des colonnes -->
      <th scope="col">Libelle</th>
      <th scope="col">Nom Long</th>
    </tr>
  </thead>
  <tbody>
    <!-- Boucle pour afficher le détail des formations -->
    {% for formation in formations %}
    <tr>
      <!-- Contenu des colonnes -->
      <td><a href="{{path('prostages_stagesParF', {id:formation.id})}}"> {{formation.libelle}} </a></td>
      <td>{{formation.nomLong}}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}

